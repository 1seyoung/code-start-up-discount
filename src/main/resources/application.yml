server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics
    endpoint:
      health:
        show-details: always


discounts:
  config-path: "src/main/resources/discounts.yml"  # 할인율 파일 경로