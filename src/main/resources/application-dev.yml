spring:
  datasource:
    url: jdbc:mysql://localhost:3306/code_start_up?useSSL=false
    username: code_start_up
    password: 000000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect

